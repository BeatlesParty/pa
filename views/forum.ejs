<%- include('header') -%>
<%- include('menu') -%>
    <div class="jumbotron" style="background-color:#8A2BE2">
        <h1>Forum</h1>
        <p>Please share your ideas for the game here on the Forum</p>
    </div>

    <form method="post" action="/forum">
      New Post: <br>
      <textarea id="post" name="post"></textarea>
      <br>
      <input type="submit">
    </form>
    <form method="post" action="/forumDelete">
      <ol>
        <ul class="list-group" id="myList">
      <% for(i = 0; i<posts.length; i++){  %>
          <div class="card-body">
          <li> <a href="/showPost/<%= posts[i]._id %>">
                <%= posts[i].userName %></a>
                <i>
               <%= posts[i].createdAt %></\/i> <br>
               <% console.dir([posts[i].userId,user._id]) %>
               <% if (posts[i].userId.equals(user._id)) { %>
                   <input type="checkbox" name="delete" value="<%= posts[i]._id %>">
               <% } %>
               <%= posts[i].post %>

           </li>
           </div>
      <% } %>
      </ul>
      </ol>
      <input type="submit" value="delete marked">
  </form>
<%- include('footer') -%>
